//Criar um programa em C que exiba os números de 500 a 1. -- Estrutura while

#include <stdio.h>

int main()
{
    // Var da estrutura while
    int contador;
    contador= 500;

    while (contador != 0)
    {
        printf("%d\n", contador);
        contador--;

        
    }

    return 0;
}